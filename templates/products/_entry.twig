{% extends "_layouts/main" %}
{% block content %}
    <div class="shop skates">
        <h1>Ice Hockey Skates</h1>
        <div class="shop-container">
            {% set entries = craft.entries().section('products').all() %}
            {% for entry in entries %}
            <div class="product">
                <img src="img/skates1.jpg" alt="skates">
                <h2>{{ entry.title }}</h2>
                <p>€ {{ entry.productPrice }}</p>
                <button>Add to cart</button>
            </div>
{% endfor %}
        </div>
    </div>
{% endblock %}