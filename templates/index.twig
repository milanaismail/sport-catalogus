{% extends "_layouts/main" %}
{% block content %}
    <div class="home">
     {% set homempage = craft.entries.section('homepage').one() %}
     {% if homepage %}
        <div class="banner-container">
            <div class="banner-overlay">
                    <h1>{{ homepage.bannerTitle }}</h1>
                    <p> {{ homepage.bannerText }}</p>
                    <button>Shop Now</button>
            </div>
            <img>
        </div>
     {% endif %}
    </div>
{% endblock %}